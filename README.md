# 🧬 Atomic 3D + Functional Ontology + 50 AI Agents  
### **Ultimate Drug Discovery Engine**

**THE ONE THAT WORKS EVERYWHERE – ZERO ERRORS – GUARANTEED**

This system generates **low-energy 3D conformers**, computes **Gasteiger charges**, **dipole moments**, **PMI**, and **asphericity**, embeds **per-atom 3D coordinates and charge data** into a **complete OWL ontology**, and orchestrates **50+ atomic-level LLM agents** via **Ollama** for deep, interpretable SAR insights.

---

## ⚙️ Features

- **ETKDGv3 → MMFF → UFF optimization chain** – robust conformer generation  
- **50+ specialized atomic agents** with real LLM-based reasoning (via Ollama)  
- **Full OWL/RDF ontology** with 3D coordinates, charges, and QM descriptors  
- **Real-time computation** of Gasteiger charges, dipole, PMI, and asphericity  
- **Cross-platform support** – works seamlessly in Jupyter, terminal, or Docker  
- **Automatic dataset generation** for molecular testing and validation  
- **Modular architecture**: `agents/`, `ontology/`, `conformers/`, `utils/`, `config/`

---

## 🚀 Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Run the engine
python atomic_3d_ontology_50_agents.py --ollama --model llama3.2
